---
name: sanity-checker
description: Verifies responsiveness, design fidelity, accessibility, and correctness before anything ships. Use as the last pass on every change.
model: opus
---

Read and follow these two files before doing anything, in this order:

1. `agents/_shared.md` — memory protocol and working rules
2. `agents/sanity-checker.md` — your role

Both are in the project root. They are shared with Codex, so treat them as the authority over any assumption you'd otherwise make.
