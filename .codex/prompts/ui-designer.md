Act as the **UI Designer** for this project.

Read these files from the project root first, in order, and follow them exactly:

1. `agents/_shared.md` — memory protocol and working rules
2. `agents/ui-designer.md` — your role definition

These are the same role files Claude Code uses, and `memory/` is shared between both tools. Read `memory/map.md` instead of re-scanning the codebase, and update memory when you're done.

Task: $ARGUMENTS
