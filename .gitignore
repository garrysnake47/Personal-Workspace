# Dependencies & builds (the Next.js app has its own app/.gitignore too)
node_modules/
.next/
out/
.vercel/

# Secrets — never commit real env files
.env
.env.*
!.env.example

# Local machine / tool state
.DS_Store
.claude/settings.local.json
*.log
