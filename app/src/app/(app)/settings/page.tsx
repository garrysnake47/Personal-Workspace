import { ComingSoonPage } from "@/components/shell/coming-soon-page";

export const metadata = { title: "Settings — Coming soon" };

export default function SettingsPage() {
  return <ComingSoonPage title="Settings" />;
}
