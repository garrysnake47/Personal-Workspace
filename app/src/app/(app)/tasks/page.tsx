import { ComingSoonPage } from "@/components/shell/coming-soon-page";

export const metadata = { title: "Tasks — Coming soon" };

export default function TasksPage() {
  return <ComingSoonPage title="Tasks" />;
}
