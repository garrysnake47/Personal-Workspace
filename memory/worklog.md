# Worklog

2026-09-23 | codex | Added optional `WorkLog.projectName` labelled “Project name / site” with an additive applied migration, Prisma generation, server validation, user-scoped create/update persistence, listing search, and responsive create/editor controls. Displayed populated project context on sprint cards, read-only timelines, and the Today dashboard. Render-verified the new field on both Work Logs creation and an existing editor without changing user data; ESLint and TypeScript pass. | app/prisma/{schema.prisma,migrations/20260923181500_work_log_project_name/migration.sql}, app/src/{actions/worklog.ts,lib/{worklogs,validation,dashboard}.ts,components/work-log/{create-work-log-form,types,work-log-editor,work-log-timeline}.tsx,app/(app)/{work-logs/**,dashboard/page.tsx}}, memory/{project,map,decisions,worklog}.md

2026-09-23 | codex | Added confirmed deletion for timeline updates entered directly on `/tickets`: user-scoped validation/action/data deletion, a labelled danger control on `TICKET UPDATE` cards, confirmation dialog, immediate timeline removal, and toast feedback. `WORK LOG` cards remain read-only and the current ticket status is unchanged. Render-verified the direct-only control and confirmation dialog without deleting existing user data; ESLint, TypeScript, and diff checks pass. | app/src/{actions/tickets.ts,lib/{tickets,validation}.ts,components/tickets/ticket-board.tsx}, app/prisma/schema.prisma, memory/{project,map,decisions,worklog}.md

2026-09-23 | codex | Changed `/tickets` to start with no default ticket, show an explicit desktop selection prompt/mobile chooser placeholder, and load details only after the user selects a ticket. Removed the exact no-change browser-verification history row (`Confirmed ticket page update; existing work-log note remains unchanged.`) from local PostgreSQL so only meaningful stored entries remain. Verified the closed initial state and click-to-open flow in the rendered app; ESLint, TypeScript, and diff checks pass. | app/src/components/tickets/ticket-board.tsx, memory/{map,decisions,worklog}.md

2026-09-23 | codex | Redesigned `/tickets` from page-wide edit cards into a compact master-detail workspace with a desktop ticket navigator, mobile ticket picker, focused title/status editor, Markdown body composer, and merged labelled timeline. Added append-only `TicketHistoryEntry` storage plus validation/action/query plumbing so every body submit creates a new immutable entry and existing `TicketWorkUpdate` work-log text is never rewritten. Applied migration `20260923153000_ticket_history_entries`; a real browser submit preserved the prior work-log entry beside the new ticket update. Verified light desktop, dark 390px mobile with zero horizontal overflow, no runtime errors, clean layout detector, ESLint, and production build. | app/prisma/{schema.prisma,migrations/20260923153000_ticket_history_entries/migration.sql}, app/src/{actions/tickets.ts,lib/{prisma,tickets,validation}.ts,components/tickets/ticket-board.tsx,app/(app)/tickets/loading.tsx}, memory/{map,decisions,worklog}.md

2026-09-23 | codex | Activated `/tickets` as a searchable/filterable ticket manager with direct title and current-status edits, protected read-only work-log context, inline save feedback, route loading UI, primary navigation, and a dashboard workflow link. Direct edits use `updateTicket` only and revalidate Dashboard; a live status-change-and-restore check confirmed the existing work note was unchanged. Browser-verified dark/light desktop and 390px mobile with zero horizontal overflow and no console errors; TypeScript, ESLint, diff check, and production build pass. | app/src/{actions/tickets.ts,app/(app)/{dashboard/page.tsx,tickets/{page,loading}.tsx},components/{shell/nav-items.ts,tickets/ticket-board.tsx}}, memory/{project,map,design,decisions,worklog}.md

2026-09-20 | codex | Removed the three-photo workspace-card section and made the four-link index the second section and `#workspace` destination. Strengthened hero text contrast, removed the About border and broadened its edge blend, brightened the coast while sharpening white quote text, and centered the contact copy. Checked 1440px light/dark and 390px dark rendered states; lint, build, and layout detector pass. | app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css, app/DESIGN.md, memory/{project,map,design,decisions,worklog}.md

2026-09-20 | codex | Reduced homepage hero/card/coast photo washes, removed the coast image filter, narrowed the About blend to 12%, constrained tablet hero copy away from the monitor, and configured Next image quality 90 for the paired photos. Checked rendered light/dark desktop and mobile views; lint and production build pass. | app/src/app/banner/banner.css, app/src/components/portfolio/portfolio-home.tsx, app/next.config.ts, memory/{design,decisions,worklog}.md

2026-09-20 | codex | Replaced Tools I Use with a four-destination editorial workspace index for live Work Logs, Resources/GitHub URLs, Notes/code ideas, and Tracker tasks/follow-ups. Replaced the About pair with front-facing laptop-screen day/night imagery, moved the coast quote right with directional contrast shading, stabilized replayable scroll motion so content never fades out on exit, improved label/body contrast and mobile/tablet navigation targets, and aligned section spacing. Browser-checked desktop and phone in both themes plus the tablet breakpoint, verified re-entry stays opacity 1, and lint/build pass. | app/src/components/portfolio/{portfolio-home,portfolio-reveal}.tsx, app/src/app/banner/banner.css, app/public/Images/portfolio/about-screen-{dark,light}.webp, app/DESIGN.md, memory/{design,map,decisions,worklog}.md

2026-09-20 | codex | Replaced the repeated hero photo in the Work Logs homepage card with a distinct journal-and-pen desk scene and matched daylight/night lighting pair. Kept the hero and other cards unchanged, retained the card's semantic copy and contrast gradient, and removed the old hero-specific crop override. Browser-checked settled light/dark at desktop and 375px mobile with no horizontal overflow; lint and production build pass. | app/public/Images/portfolio/worklog-{dark,light}.webp, app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css, app/DESIGN.md, memory/{design,map,decisions,worklog}.md

2026-09-19 | codex | Restored the coast photo's sunset, rocks and water behind the quote by removing the compounded darkening, cropping toward the horizon, and adding theme-specific quote filter/shade tokens. Checked the rendered quote at 1440px and 375px in light/dark with no horizontal overflow; preserved white text readability. | app/src/app/banner/banner.css, app/DESIGN.md, memory/{design,decisions,worklog}.md

2026-09-19 | codex | Removed the About image's inset divider and masked both paired photo layers into the existing homepage ground, horizontally on desktop and vertically on mobile. Browser-checked the formerly hard seam in light/dark at 1440px and the stacked image at 375px; no horizontal overflow. | app/src/app/banner/banner.css, app/DESIGN.md, memory/{design,decisions,worklog}.md

2026-09-19 | codex | Made the public homepage hero a full visible viewport (`100dvh` with fallback) and added reusable Motion reveals for hero copy, workspace cards, about, tools, quote and contact. Each element resets after leaving view and animates again when scrolling either direction; the menu stays static. SSR/no-JS content defaults visible, and reduced-motion removes the travel. Verified desktop 1440x1000 and mobile 375x812, both themes, zero horizontal overflow, and card exit/re-entry states in the browser; lint and production build pass. | app/src/components/portfolio/{portfolio-home,portfolio-reveal}.tsx, app/src/app/banner/banner.css, app/DESIGN.md, memory/{map,design,decisions,worklog}.md

2026-09-19 | codex | Installed `motion` and the Skiper UI 40 registry component via shadcn after adding a scoped `components.json`; used Motion for the theme-toggle icon exchange and Skiper's focus/reduced-motion-aware underline on two secondary homepage links, with required footer attribution. Improved the header scrim over bright photography. Browser-checked 1440px and 375px in light/dark with no horizontal overflow; lint and production build pass. | app/{package.json,package-lock.json,components.json,DESIGN.md}, app/src/components/{portfolio/{portfolio-home,portfolio-theme-toggle}.tsx,ui/skiper-ui/skiper40.tsx}, app/src/app/banner/banner.css, memory/{map,design,decisions,worklog}.md

2026-09-19 | codex | Replaced the mismatched light portfolio scenes with five daylight lighting edits generated directly from the dark canonical frames, preserving hero/person/card/about/coast composition across theme changes. Updated the live image references, strengthened daylight rules, reserved card aspect ratio, added keyboard-equivalent image feedback, removed radius-changing focus CSS, and documented the canonical-pair rule. Prompt provenance embedded in all new assets. | app/public/Images/portfolio/*-light-v2.png, app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css, app/{DESIGN.md,.impeccable/design.json}, memory/{design,map,decisions,worklog}.md

2026-09-19 | codex | Rebuilt `/` and `/banner` as the supplied complete photo-led personal workspace layout with paired day/night hero, project, about and coast assets; added a persisted 44px theme toggle, real mobile navigation, responsive single-column layouts, self-hosted Cormorant Garamond quote type, semantic live-route cards, and removed placeholder contact/social destinations. Embedded provenance in all 10 user-supplied rasters. Browser-verified light/dark at true 375x812 and 1440x1000 with zero horizontal overflow; ESLint, production build, detector, independent Impeccable review (`ship`) and design documentation pass. | app/src/components/portfolio/**, app/src/app/{(marketing)/page.tsx,banner/**,layout.tsx,fonts/**}, app/public/Images/portfolio/**, app/{DESIGN.md,.impeccable/design.json}, memory/{project,map,design,decisions,worklog}.md

2026-09-19 | codex | Removed the `/banner` timeline and “Start with today” chapters plus their dead component/styles/navigation anchor, leaving a direct hero → connected toolkit → closing-scene flow. Centered the closing statement, retuned all three wooden-board labels with Bricolage Grotesque plus board-specific placement/rotation, and replaced repeated text slides with distinct hero/toolkit/closing choreography. ScrollTrigger now restarts each chapter on forward or backward entry and resets after leaving back; menu remains static and reduced-motion remains final-state. Browser-verified light/dark at desktop, 390px with no overflow, and measured mid-animation → final-state replay after scrolling away/back. ESLint, TypeScript, production build and Impeccable detector pass. | app/src/app/banner/{page.tsx,banner.css}, app/src/components/banner/{banner-nav,banner-closing,banner-motion}.tsx, app/src/components/banner/banner-story.tsx (deleted), memory/{design,decisions,map,worklog}.md

2026-09-18 | codex | Rebuilt `/banner` into four visually distinct scroll chapters: replaced the flat capability row with a connected Phosphor-icon hub, separated the append-only timeline from a calmer start CTA, and added a matched generated sunrise/night closing landscape with smaller semantic copy and sign labels. Added scoped GSAP section/element reveals while keeping the menu static; fixed transform-driven card collisions and theme-image visibility. Rewrote login/register support stories so they are meaningfully different, replaced neon dark auth teal with deep emerald, and repaired compact mobile auth spacing. Browser-verified light/dark at 390 and 1440 with no horizontal overflow; Impeccable provenance scan, ESLint, TypeScript and production build pass. | app/public/Images/banner/**, app/src/app/{banner/**,(auth)/auth-motion.css}, app/src/components/{banner/**,auth/auth-shell.tsx}, memory/{design,decisions,map,worklog}.md

2026-09-18 | codex | Removed all GSAP entrance motion from the `/banner` navigation, slowed and separated the hero scene/copy/action/benefit sequence so groups no longer arrive together, and added a reference-inspired responsive capability section below the hero. Kept the new section inside the scoped warm-ivory/teal light/dark palette and limited its content to live product capabilities. Browser-verified light/dark at 390, 768, 1024, 1280 and 1440 with no horizontal overflow or console errors; ESLint, TypeScript and production build pass. | app/src/app/banner/{page.tsx,banner.css}, app/src/components/banner/{banner-hero,banner-nav,banner-motion,banner-toolkit}.tsx, memory/{design,decisions,map,worklog}.md

2026-09-17 | codex | Removed the Daily Log, ASU and Meeting Notes overlays from `/banner`, then deleted their component, token/style system and dead SVG icons plus the obsolete drawn-scene fallback. Replaced the soft photo pair with deep-focus day/night assets (`scene-workspace-v3.png`, `scene-workspace-dark-v2.png`) that retain the notebook and mug lettering, and removed image-layer scaling so the pixels stay crisp. Reworked GSAP load motion from bottom-to-top/blur entrances into a horizontal clip-mask reveal with no vertical travel; CTAs now use opacity plus a subtle scale settle only, eliminating load blur. Browser-verified the finished light and dark compositions and a mid-load sharp-button frame; Impeccable detector, ESLint, TypeScript and production build pass. | app/public/Images/banner/{scene-workspace-v3.png,scene-workspace-dark-v2.png}, app/src/components/banner/{banner-hero,banner-icons,banner-motion,banner-scene}.tsx, app/src/components/banner/{banner-cards,banner-scene-art}.tsx (deleted), app/src/app/banner/banner.css, memory/{design,decisions,map,worklog}.md

2026-09-17 | codex | Redesigned and repositioned the `/banner` product-card cascade to match the supplied close-up: larger widths/type/padding, refined glass depth, Daily leading, ASU foremost lower-right, and Meeting Notes tucked lower-left at 25.2vw with a visible gap above the notebook. Replaced the IntersectionObserver reveals with one scoped GSAP/@gsap/react page-load timeline covering navigation, all hero/card/benefit text, card shells and scene, with cleanup and reduced-motion bypass. Added a separately generated teal-black/amber night photograph and cross-fade dark mode instead of filtering daylight. Browser-verified final light/dark compositions and GSAP settling; Impeccable layout detector clean; lint, TypeScript and production build pass. | app/package{,-lock}.json, app/public/Images/banner/scene-workspace-dark.png, app/src/components/banner/{banner-hero,banner-motion,banner-scene}.tsx, app/src/app/banner/banner.css, memory/{design,decisions,map,worklog}.md

2026-09-16 | codex | Corrected the `/banner` composition after live screenshot review: lifted Meeting Notes from 29.6vw to 27.5vw so it has a clear gap above the notebook, increased its positive resting rotation to follow the book direction, and precisely edited the background photo to restore gold “Better Workdays” notebook lettering plus cream “Good Notes Brighter Days” mug script. Switched `BannerScene` to the versioned `scene-workspace-v2.png` URL to bypass the stale Next Image cache. Browser-verified the gap and both labels; lint, TypeScript and production build pass. | app/public/Images/banner/scene-workspace-v2.png, app/src/components/banner/{banner-hero,banner-scene}.tsx, app/src/app/banner/banner.css, memory/{design,decisions,map,worklog}.md

2026-09-16 | codex | Removed the `/banner` social-proof copy and avatar row, deleted the now-unused avatar component and styles, and replaced the one-time copy/card entrances with a progressive `IntersectionObserver` system. Scene, nav, copy groups, all three 3D cards and each benefit now reveal independently on page load and replay on viewport re-entry; CSS individual translate preserves card perspective/hover transforms, content remains visible without JS, and reduced-motion renders the final state. Browser-verified the updated live route; lint, TypeScript and production build pass. | app/src/components/banner/{banner-hero,banner-nav,banner-cards,banner-motion}.tsx, app/src/components/banner/banner-avatars.tsx (deleted), app/src/app/banner/banner.css, memory/{design,decisions,map,worklog}.md

2026-09-16 | codex | Matched the `/banner` Daily Log, ASU and Meeting Notes cards to the supplied 3D reference: increased glass opacity/blur, added a top-to-bottom translucent surface gradient, inner highlight rim and layered contact/cast shadows, gave each card its own resting perspective, and corrected z-order so ASU sits above Meeting Notes. Fixed the entrance animation's persistent fill mode so hover can straighten/lift cards, then caught and corrected a CSS cascade where the new highlight positioning overrode desktop absolute placement. Browser-verified resting and hover states in light mode plus card depth in dark mode; lint, TypeScript and production build pass. | app/src/app/banner/banner.css, memory/{design,worklog}.md

2026-09-16 | codex | Replaced the visibly stale drawn `/banner` scene with a new background-only photoreal workspace asset while keeping navigation, copy, avatars, all three product cards, icons and controls as independent DOM/SVG layers. Simplified `banner-scene.tsx` to Next Image, added a scoped dark teal/amber treatment, corrected the nav CTA to `/register`, and added reduced-motion-safe copy/card entrances plus card, search, CTA and feature hover feedback. Diagnosed the user's unchanged screenshot as a stale Next dev process and route cache; restarted port 3000 after clearing only `.next`, then browser-verified the updated route in light and dark. ESLint, TypeScript and production build pass; `/banner` is statically generated. | app/public/Images/banner/scene-workspace.png, app/src/components/banner/{banner-scene,banner-hero,banner-nav}.tsx, app/src/app/banner/banner.css, memory/{design,decisions,map,worklog}.md

2026-09-16 | claude | Wired the /banner scene generator to a real GEMINI_API_KEY the user supplied, and hit a hard stop that is worth recording: the key is VALID (gemini-3.6-flash returns text fine) but every image-capable model on it — gemini-2.5-flash-image, gemini-3-pro-image, gemini-3.1-flash-image, gemini-3.1-flash-lite-image and the previews — returns 429 with `free_tier_requests, limit: 0`. Gemini image OUTPUT has no free tier; the Cloud project behind the key needs billing enabled. Also note gemini-2.5-flash itself is now 404 for new keys ('no longer available to new users', use gemini-3.6-flash). Along the way the script lost its google-genai dependency: macOS ships an externally-managed Python (PEP 668) so `pip install` fails, and the REST endpoint does the same job with urllib and no venv. `app/scripts/generate-banner-scene.py` is ready to run the moment billing is on; `banner-scene.tsx` picks the PNGs up with no code change. | app/scripts/generate-banner-scene.py, memory/worklog.md

2026-09-16 | claude | `/banner` follow-up after the user showed a screenshot where the cards collided: the first pass gave the floating cards MINIMUM PIXEL WIDTHS, so on a ~1100px window they stopped being the comp's 23%/21%/21% of the canvas, grew past their slots and stacked on each other. Rewrote the page on one rule — from lg up every size is `clamp(floor, <comp px>/1648*100vw, ceiling)`, i.e. a fraction of the comp's 1648x928 canvas — so the whole thing is a scaled copy of the comp at any width instead of re-flowing against it. That moved type, padding, radii, tiles, check circles, pills, nav height, avatars and feature icons into `banner.css` as `.bn-*` classes with a fixed-px tier below lg (a 1.09vw card title is 4px on a phone) and a vw tier above. Card TOPS are now vw too (9.7 / 23.7 / 32), not percentages of section height, so the collage keeps the comp's internal proportions on any window aspect; widths and lefts are the comp's own fractions. Copy measure widened to 56% — Plus Jakarta Sans sets wider than the comp's face, and at 51% the headline broke to three lines. Also split the scene: `banner-scene.tsx` is now a server component that looks for `public/Images/banner/scene-{light,dark}.{jpg,jpeg,png,webp,avif}` and uses the photographs when both exist, falling back to the drawn SVG (moved to `banner-scene-art.tsx`) when they do not — the user asked for real images and this session has NO image generation available (no GEMINI/ATLASCLOUD/MUAPI key, no image MCP, no ImageMagick), so the slot is wired and waiting. Added `app/scripts/generate-banner-scene.py` — a direct gemini-2.5-flash-image call (16:9, one prompt per theme, same objects in both, left third left empty for the copy) that writes straight into `public/Images/banner/`; it runs as far as `GEMINI_API_KEY not set`, which is the only thing missing. Verified in both themes at 1440, 1100 and 390. ESLint and tsc --noEmit pass. | app/src/app/banner/banner.css, app/src/components/banner/{banner-hero,banner-nav,banner-cards,banner-avatars,banner-scene}.tsx, app/src/components/banner/banner-scene-art.tsx (renamed from banner-scene.tsx), app/scripts/generate-banner-scene.py (new), memory/{map,decisions,worklog}.md

2026-09-16 | claude | Finish handoffs for the homepage redesign. Documenter wrote `app/DESIGN.md` (token-bearing, both themes) and `app/.impeccable/design.json` (schemaVersion 2) from the built code, and flagged five things outside its write boundary. Acted on the two real defects: (1) three contrast comments in globals.css were aspirational rather than measured — text-muted was labelled 8.1:1 but measures 6.87, text-subtle 5.4 -> 4.89, primary-strong 4.8 -> 4.50; comments corrected, and `--c-primary-strong` moved #4a7c18 -> #45731a (4.50 -> 5.06:1) because a link/small-label colour sitting exactly on the AA line is not a margin. DESIGN.md and design.json re-synced to the new value. (2) `shadow-md` on the marketing mobile menu was the only shadow on the shipped surface and contradicted the world's border-only elevation rule; removed, and a live audit now counts ZERO shadowed elements under `.marketing-document`. Also deleted the superseded `hero-banner.tsx` and its `.hero-*` CSS. NOT acted on, reported instead: `pricing.tsx`, `faq.tsx` and `app-mock.tsx` are now imported by nothing, as is a chunk of the old world's CSS (`.pw-*`, the `data-reveal` layer, `.marketing-snap-section`) — deleting those is a product call, not a design one, since they are plausible components for pages that do not exist yet. Lint, tsc, production build all clean. | app/{DESIGN.md,.impeccable/design.json} (new), app/src/app/globals.css, app/src/components/marketing/marketing-nav.tsx, app/src/components/marketing/hero-banner.tsx (deleted), memory/worklog.md

2026-09-16 | claude | Built `/banner` — a standalone page reproducing the two supplied reference comps (light + dark) of a work-log hero. Route lives outside both the app shell and the marketing frame (`app/src/app/banner/`), and is added to `PUBLIC_ROUTES` so it renders without a session. Palette is scoped: `banner.css` declares ~70 `--bn-*` variables on `.banner-page` with a `.dark .banner-page` override, so the comp's warm ivory/night-studio colour story never leaks into the app's slate+teal tokens, and the existing `.dark` class toggle re-lights the whole page. The photographic desk is rebuilt as ONE inline SVG (`banner-scene.tsx`, viewBox 1648x928 = the comp's own canvas) whose every fill is a token — notebook, pen, mug with handwritten glaze, plant, Focus/Build/Grow blocks, laptop corner, framed print, blurred foliage, warm lamp gradient and vignette — which is why dark mode is a re-lit room rather than a filtered copy. Nav, copy, the three floating glass cards and the benefit row are real DOM; all 12 icons are hand-drawn inline SVG on currentColor. Three traps found by rendering it: (1) `h-[46%]` on the mobile scene band measured the SECTION, which is as tall as its stacked content below lg, so the mug alone filled the phone — now a fixed 420px band; (2) the comp's own card offsets stack the ticket on the meeting card at 1024x720, so lg gets pulled-apart positions and narrower clamps while xl carries the comp's exact fractions, plus a `min-h-[max(100dvh,780px)]` floor; (3) the comp's greys are 3.6:1 and 3.3:1 on the cream ground at 13px, so `--bn-muted` and `--bn-card-meta` were darkened to ~4.9:1. Avatars are flat SVG portraits, not photos — the repo has no headshots. Browser-verified in both themes at 1440, 1280, 1024, 768 and 390: no horizontal overflow, one h1, one nav. ESLint and tsc --noEmit pass. | app/src/app/banner/{page.tsx,banner.css} (new), app/src/components/banner/{banner-hero,banner-nav,banner-scene,banner-cards,banner-avatars,banner-icons}.tsx (new), app/src/lib/auth.config.ts, memory/{map,design,decisions,worklog}.md

2026-09-16 | claude | Complete redesign of the marketing homepage through the newly installed **impeccable** skill (`npx skills add pbakaus/impeccable`, now at `.agents/skills/impeccable`, symlinked into `.claude/skills`). Ran the skill's real flow rather than freestyling: init -> PRODUCT.md, concept-seed roll (seed `c0fbca57`), a served decision page the user answered, direction contract in a surface brief, craft-floor pass, mechanical detector, then two finish-review rounds by a fresh reviewer. User's binding answers: this page is a REAL pitch aimed at strangers (Persuade), product surfaces are DRAWN in DOM+tokens not screenshotted, and NOTHING was preserved — they asked in their own words for "complete new colour palette, new design". The roll assigned a court-reporting direction; the user overrode it with the presented IMPECCABLE'S PICK, **The Commit Graph** — the working day rendered as version-control history, because a commit log is already proof that a record appends and never overwrites. New marketing-only world: ink ground with a violet cast (`#13111c` dark / `#f3f2f6` light), plotter-lime accent, deletion red used EXACTLY once on the page (struck, resolving); Bricolage Grotesque display + Archivo prose + Martian Mono for real data only. Scoped by redefining the `--c-*` raw tokens on `.marketing-document`, so every existing token utility renders the new world and the authenticated app keeps its teal + Plus Jakarta Sans untouched. New components: `commit-rail.tsx` (the spine + a real merge curve), `amend-attempt.tsx` (the signature interaction — pressing "Amend this entry" does NOT edit; a dated correction appends and the original stays legible), `day-in-the-log.tsx` (drawn product surface, labelled illustrative). Removed on craft-floor orders: the eyebrow above the heading (an outright ban), 01/02/03 step numbers, the nav/footer's thick coloured offset border, the person illustration, and a dead `--ease-back-out` bounce token. Reviewer round 1 returned `fix` with 8 findings including two real truth violations I shipped (the words "for the team" in a single-user product, and a "free" pricing claim PRODUCT.md does not supply); round 2 scored 7/8 and correctly failed the spine; round 3 shipped. ESLint, tsc --noEmit, production build and detector all clean. | app/src/components/marketing/{commit-rail,amend-attempt,day-in-the-log}.tsx (new), app/src/app/(marketing)/page.tsx, app/src/components/marketing/{marketing-nav,marketing-footer}.tsx, app/src/app/{layout.tsx,globals.css}, app/PRODUCT.md (new), app/.impeccable/**, memory/{design,decisions,map,worklog}.md

2026-09-16 | claude | Hero banner follow-up: the first pass was structurally right but read as the same two-column hero it replaced, so the change was invisible. Scale was the problem, not structure. Artwork went from framed (lg:h-[86%], bottom-aligned) to oversized and bleeding past both the right and bottom edges (lg:h-[104%] w-[62%], right/bottom -6%, bottom mask eating the crop). Headline moved off the project's stepped scale — which tops out at 72px — to a fluid clamp(2.625rem,5.2vw,4.5rem) at leading-[1]/tracking-[-0.03em] with text-balance. Copy column widened to lg:max-w-[44rem] so "Write the day down" sets on one line at desktop with the italic accent line under it (a 34rem measure orphaned "down" on its own line); the paragraph kept a separate max-w-[44ch] so the body measure did not grow with the column. Browser-verified at 1440 light + dark and 375; ESLint, tsc --noEmit and the production build pass. | app/src/components/marketing/hero-banner.tsx, memory/{design,worklog}.md

2026-09-16 | claude | Replaced the homepage hero with a full-bleed banner modelled on the CreativaX studio reference, on this project's own tokens and copy. New server component `components/marketing/hero-banner.tsx`: edge-to-edge min-h-dvh section, copy held in a left column, the existing person-writing-work-log.png anchored bottom-right and bleeding off the edge, and three --c-bg gradient masks (left wedge lg:58%, top h-64, bottom h-48) that keep the copy readable and seat the banner in the page. Headline splits the reference's way — "Write the day down" in extrabold Jakarta over "as you live it." in font-light italic Jakarta at primary-strong. Instrument Serif was added as a `font-display` token for that line, then removed on review: weight + slope in one family carries the accent, and the product stays at two faces. That did require loading Jakarta's italic file (`style: ["normal","italic"]`) — a synthesised oblique is an obvious shear at 52px. Reference's avatar stack became the M/T/W/T working week (single-user product, so no "trusted by 200+ brands"). No client JS: artwork drift and the CTA arrow are CSS, both off under prefers-reduced-motion. Found and fixed a real trap along the way — this project deletes Tailwind's `sm` breakpoint (`--breakpoint-*: initial`, only xs/md/lg/xl), so `sm:` utilities compile to nothing and fail silently; the first pass had nine of them and the CTA row never left column layout. Browser-verified at 1440 and 375 in light and dark, hero anchor scroll confirmed, no console errors; ESLint, tsc --noEmit and the production build pass. | app/src/components/marketing/hero-banner.tsx (new), app/src/app/(marketing)/page.tsx, app/src/app/layout.tsx, app/src/app/globals.css, memory/{design,decisions,worklog}.md

2026-09-16 | claude | Full redesign pass on /dashboard cards, queried against ui-ux-pro-max first (it independently returned #0D9488 teal + Plus Jakarta Sans + Flat Design, confirming the existing tokens; its landing-page pattern result was disregarded as off-target for an authed dashboard). Restructured six equal-weight cards into four bands — Today full-width with a teal top rule and the only CTA, then Needs you + Recent ticket work, then This sprint + Tickets by stage, then Library demoted from a card to a nav strip. Added a local Card shell so panels share padding/radius/border/heading; card titles went from text-lg to text-sm on purpose. Lists are now divide-y with -mx-2 hover rows instead of a bordered box per row. Also fixed seed-fill description repetition (rotate globally per stage, 3 lines each) after the rendered page showed one sentence three times. VERIFIED the authenticated render this time: minted a 1-hour dev session JWT with the app's own AUTH_SECRET for demo@workspace.local, fetched /dashboard (HTTP 200), and confirmed the new structure, the stage shares summing to 100%, and the old patterns gone; token deleted after. ESLint and tsc --noEmit pass. Still not screenshotted — no browser session. | app/src/app/(app)/dashboard/page.tsx, app/prisma/seed-fill.ts, memory/{design,decisions,worklog}.md

2026-09-16 | claude | Added a current-sprint top-up seed and fixed a date bug in the existing ones. New `prisma/seed-fill.ts` (`npm run db:seed:fill [email] [days]`) fills trailing weekday gaps including today: work log + 4 meeting notes + 2-3 ticket updates per day, plus 4 tracker entries. Additive and idempotent — skips dates that already have a log, upserts on (ticketId, workLogId), and advances each ticket's stage FORWARD ONLY by reading its real current status (seed-bulk restarts its arcs at step 0, which can walk a Released ticket back to InProgress). Fixed the shared `daysAgo` bug in seed-bulk.ts and seed.ts: both read getUTC* components for "today" while the app keys work logs on the LOCAL calendar day, so at UTC+5:30 every evening run landed a day early and left the dashboard's Today card empty. Ran it for demo@workspace.local — 2 new work logs (15, 16 Sep), 4 ticket updates, 4 tracker entries; sprint counters and the Today card now read non-zero, and tickets span all five stages. ESLint and tsc --noEmit pass. | app/prisma/{seed-fill.ts (new),seed-bulk.ts,seed.ts}, app/package.json, memory/{decisions,worklog}.md

2026-09-16 | claude | Acted on a six-point design review of /dashboard. (1) Replaced the "Tickets by stage" proportional bars with a count + share-of-total list: the fill was surface-3 on a surface-2 track at 1.13:1, which read as a loading skeleton, and it normalized to the busiest stage rather than the total, so the largest pile always rendered as a full bar. Removed the now-unused busiestStage. (2) Stat micro-labels moved from text-subtle to text-muted — 4.34:1 -> 6.92:1 light, 5.89:1 -> 9.88:1 dark, clearing AA at 11px. (3) greeting() gained a sub-5am branch ("Working late"); hour<12 meant 00:57 read "Good morning." (4) The sprint card collapses to one line when the sprint has no activity yet, instead of three large zeros. (5) Dropped the Today card's date chip — the page header above it already states the full date. (6) Removed the dashed boxes from all four empty states so the card is the only frame. ESLint, tsc --noEmit and the production build pass; the authenticated render was NOT browser-verified (preview pane has no session, Chrome extension not connected). | app/src/app/(app)/dashboard/page.tsx, memory/{design,decisions,worklog}.md

2026-09-16 | claude | Repainted the three homepage "how it works" illustrations onto the teal brand ramp — they were still carrying the rejected brand blue #017EFD. Recolored meeting-notes-cards.svg and ticket-history-timeline.svg (blue -> #0D9488, blue-tinted card fill -> #F4FCFA, old navy #0A1B33 -> text token #0F172A, blue-grey spine -> border token #E2E8F0); semantic status tints in the timeline were already the status-*-bg tokens and were left alone. Replaced checklist-cards.png (709KB soft-3D raster with baked-in blue checkmarks, unrecolorable and stylistically off from the other two) with a hand-drawn flat checklist-cards.svg in the same idiom, so the step no longer loads 709KB of raster. The PNG is left on disk but is now unreferenced — it was a user-supplied original, so deleting it is the user's call. Browser-verified all three at 1440 and 375 in light mode, no console errors. | app/public/Images/{meeting-notes-cards.svg,ticket-history-timeline.svg,checklist-cards.svg (new)}, app/src/app/(marketing)/page.tsx, memory/{design,decisions,worklog}.md

2026-09-14 | codex | Built the active Resources workspace: added Website/App/Link resource types and additive Prisma migration, exposed Resources beside Work Logs in the sidebar, wired the existing secured resource actions to a type-aware add form, and replaced the Coming Soon page with a responsive searchable index. Resource rows expand for URLs, commands, notes, tags, copy, and delete actions. Flattened the presentation after review so it reads as a focused library rather than a dashboard nested inside the app shell; sidebar uses a stronger token surface/header treatment. Browser-rendered the initial desktop page and composer before the browser tool hit its usage limit; ESLint, TypeScript, diff check, and production build pass. | app/prisma/{schema.prisma,migrations/20260913153000_resource_library_types/migration.sql,migrations/20260913173755_resource_library_types/migration.sql}, app/src/{app/(app)/resources/page.tsx,components/resources/resource-library.tsx,components/shell/{app-sidebar,nav-items}.ts(x)}, memory/{project,map,decisions,worklog}.md

2026-09-13 | codex | Focused the product on homepage, login, registration, and Work Logs: reduced the sidebar to Work Logs with a distinct neutral surface; simplified the top bar to New work log and account/theme/logout; redirected successful auth to Work Logs; replaced Dashboard, Tickets, Tasks, Notes, Reports, Links, Resources, Profile, and Settings with a shared Coming Soon page; removed the obsolete dashboard queries/components and dashboard revalidation; narrowed public/auth copy to the active workflow. Browser-verified authenticated and guest routes in light and charcoal dark mode with no overflow or console errors; ESLint, TypeScript, diff check, and production build pass. | app/src/{actions/**,app/**,components/**,lib/**}, memory/{project,map,decisions,worklog}.md

2026-09-13 | codex | Repaired the dashboard Prisma crash without rewriting ticket history: the database enum migration is additive, legacy statuses are normalized into the five-stage workflow at query boundaries, and the regenerated client now reads both old and new values. Corrected active-ticket ordering, removed the client-rendered script warning, excluded weekends from sprint timelines, and tightened registration so every field and action fits a 1280x720 viewport without scrolling or overlap. Applied the local migration; browser-checked every authenticated route plus guest login/register in light and true charcoal dark mode with no page errors or horizontal overflow. ESLint, TypeScript, diff check, and production build pass. | app/prisma/{schema.prisma,migrations/20260913010000_ticket_sprint_statuses/migration.sql}, app/src/{lib/{workflow-status,tickets,worklogs,validation}.ts,app/{layout.tsx,(app)/**},components/{auth/**,dashboard/**,tickets/**,ui/**,work-log/**}}, memory/{map,decisions,worklog}.md

2026-09-13 | ui-designer | Refreshed app-shell light/dark palette from the UI/UX Pro Max Flat Design + dark-mode SaaS guidance: neutral slate surface ladder, accessible blue action tokens, flatter cards, and explicit empty-dashboard treatment; no component markup or logic changed. | app/src/app/globals.css, memory/design.md, memory/worklog.md

Newest at top. Format: `YYYY-MM-DD | agent | what changed | files`

2026-09-19 | codex | Replaced the former `/` marketing page with the complete three-chapter `/banner` experience; separated login into a returning-user progress/latest-log composition and registration into an emerald connected-workspace canvas with no repeated checklist; rebuilt `/dashboard` as an asymmetric command centre with a dominant graphite Today panel, focus queue, sprint/workflow momentum, activity trail and library dock; changed the app brand link to `/dashboard`. Render-checked `/`, login and registration at 1440 and 390 in light/dark, with no browser console errors; ESLint, TypeScript and production build pass. The authenticated dashboard compiled but was not browser-rendered because the saved local browser session had expired and no credentials were entered. | app/src/app/{(marketing)/**,(auth)/auth-motion.css,(app)/dashboard/page.tsx,banner/page.tsx}, app/src/components/{auth/**,shell/app-nav.tsx}, memory/{project,map,design,decisions,worklog}.md


2026-09-13 | codex | Completed a full-site quality pass: restored the useful data-backed dashboard; replaced Tasks, Notes, Reports, Links, Resources, Profile, and Settings placeholders with real authenticated views; strengthened dark-mode accent/border contrast and exposed theme controls on marketing/auth routes; raised interactive targets and visible micro-labels; added contained sprint scroll snapping; repaired save/search hook lint violations; removed live Google Font build dependencies; and made the stable webpack builder the production default. TypeScript, ESLint, diff check, and `npm run build` all pass. | app/{package.json,src/app/{globals.css,layout.tsx,(app)/**},src/components/{auth/**,dashboard/**,marketing/**,shell/**,theme/**,tickets/**,ui/**,work-log/**}}, memory/{design,map,worklog}.md

2026-09-12 | sanity-checker | PASS after live rendered verification at 360/768/1024/1440: exactly three homepage snap targets, no horizontal overflow, desktop nav centred within 1px, distinct token section colours, smooth forward/backward/deep-link section navigation with matching aria-current, animated homepage/menu/auth states, and reduced-motion final-state CSS. Login and registration are concise and visually distinct; one supplied-credential POST stayed on clean `/login`, exposed no query parameters, and returned the generic inline `Incorrect email or password` error. Browser console clean; TypeScript and targeted ESLint pass. Initial backward-nav and 36px menu-toggle failures were retested after the frontend fix (scrollTop returns to 0; toggle is now 44x44). | memory/worklog.md

2026-09-12 | frontend-dev | Fixed nested homepage hash navigation: all section links retain real hrefs/history while explicitly smooth-scrolling the owning `.marketing-scroll` container forwards and backwards, with auto behavior for reduced motion and hash/popstate synchronization. Increased the mobile menu control to the required 44x44 target. | app/src/components/marketing/marketing-nav.tsx, memory/worklog.md

2026-09-12 | frontend-dev | Added homepage-only three-stop smooth snapping, full-viewport sections, truly centred desktop menu, animated section-aware aria-current state, animated mobile menu, and existing-token section colours; folded footer into section three. Replaced duplicate auth marketing panels with minimal login and distinct register presentation, preserving validation/accessibility and adding method=post privacy fallback aligned with the unchanged auth contract. TypeScript and targeted ESLint pass; rendered pass handed to Sanity Checker. | app/src/app/{globals.css,(marketing)/**,(auth)/**}, app/src/components/{marketing/**,auth/**}, memory/{map,worklog}.md

2026-09-12 | backend-dev | Repaired LAN-hosted credential submission: allowed the exact `192.168.31.130` dev origin and matching `:3000` Server Action origin so Next 16 no longer blocks hydration/action requests; added server-side path-relative callback sanitisation without weakening Zod, bcrypt, generic auth errors, or JWT sessions. Live LAN submission now reaches Auth.js; the existing reported account has a hash but the supplied password genuinely does not match, so account recovery remains an explicit user decision. | app/next.config.ts, app/src/actions/auth.ts, memory/{map,decisions,worklog}.md

2026-09-12 | ui-designer | Defined the requested three-section homepage interaction system from UI/UX Pro Max: shared 150/200/350/400ms motion tokens, true centred desktop nav, non-colour-only active section state, scoped three-stop smooth snapping, existing-token section colour transitions, and simplified/distinct login vs registration content direction. No component markup or app logic changed. | app/src/app/globals.css, memory/{design,decisions,worklog}.md

2026-09-12 | frontend-dev | **Built the real /dashboard** (it was `return null` — a blank page). Deliberately NOT the spec's seven-card activity dashboard: per the user ("keep the colors only remove section which are not required", "just general info about my sites what are the things present") it is a calm workspace overview — greeting + date, the Today's Work Log hero as the single primary action (Continue vs Create, with meetings-noted / tickets-touched metrics), six "In your workspace" count tiles (Work Logs / Tickets / Tasks / Notes / Links / Resources, each linking into its section, each with a real zero state), and a 5-row Active Tickets list carrying `TicketStatusBadge` + the 3px status left rule. **Today's Meetings, Recent Notes, Quick Links and Recent Activity were NOT built and their stub components deleted.** Counts come from a dashboard-local `queries.ts` doing real `COUNT`s (the `list*` actions are paginated, so `.length` would have lied); today's log is found with a read-only `listWorkLogs({from,to})` probe so the page never creates a log just by being viewed; every `ok:false` renders `CardError` for that block only. Motion is CSS-only (NO GSAP, no new dep) in a CSS module, timings taken from ui-ux-pro-max `motion.csv` rows 7 (Stagger List/Subtle -> 250ms, power1.out, 30ms per item), 1 (Hover Micro-interaction/Subtle -> 150ms, -1px) and 4 (Scroll Reveal/Subtle -> the 320ms count-up); transform/opacity only; `prefers-reduced-motion` restated locally because `.rise` starts at opacity 0. All normal-size blue is `primary-strong` (CTA fills, footer links, eyebrow) — `primary` only as large fill/icon. Verified: tsc + build pass; seeded volume read straight from Postgres (47/13/14/9/11/11, 5 active tickets, no log for today). **NOT verified in the browser** — no session was obtainable without typing a password, and minting a dev session token was denied by the permission classifier; a signed-in visual pass at light/dark/360 is still outstanding. | app/src/app/(app)/dashboard/{page.tsx,queries.ts}, app/src/components/dashboard/{workspace-counts,count-up,active-tickets-card,today-work-log-card,today-work-log-cta,dashboard-card,types}.tsx, app/src/components/dashboard/dashboard-motion.module.css, deleted app/src/components/dashboard/{quick-links-card,recent-activity-card,recent-notes-card,tasks-card,todays-meetings-card}.tsx, memory/{map,decisions,worklog}.md

2026-09-12 | ui-designer+frontend-dev | Redesigned /login + /register as a real auth experience: two-pane layout (form column + brand-blue panel with value bullets and a placeholder quote) collapsing to one column with a compact highlight strip below `lg`; auth-scoped 44px field/input primitives with a reserved message slot (no layout shift when an error appears), leading Lucide icons, brand-blue focus edge; password visibility toggle (aria-label + aria-pressed, distinct label per field); live password-strength meter (scaleX, never width) and instant "passwords match" check on register; focusable error summary + focus moved to the first invalid field on failed submit. Motion is CSS-only (NO GSAP, no new deps) with timings/easings taken from the ui-ux-pro-max `motion.csv` rows 1/4/7/8: staggered 300ms/60ms entrance, 200ms error reveal, 150ms -1px button hover; `prefers-reduced-motion` asserted locally because `.auth-rise` starts at opacity 0. Panel uses `primary-active` (7.5:1 for white body copy) and CTAs `primary-strong` — `primary` only for large fills/decoration. Verified: tsc + build pass, no horizontal scroll at 360, empty-submit renders all inline errors with focus moved to the first invalid field, tab order matches reading order, reduced-motion rule confirmed in the parsed CSSOM. `ui/` primitives, marketing and app routes untouched. | app/src/app/(auth)/{layout.tsx,auth-motion.css,login/page.tsx,register/page.tsx}, app/src/components/auth/{auth-shell,auth-panel,auth-field,password-input,password-strength,login-form,register-form}.tsx, memory/{map,decisions,worklog}.md

2026-09-12 | frontend-dev | **ROUTING: `/` is now the public marketing homepage; the dashboard moved to `/dashboard`.** Built the whole marketing page (pill nav, hero, DOM-drawn dashboard screenshot band, 3 alternating feature blocks, single-plan pricing with monthly/yearly toggle, placeholder testimonials, accessible FAQ accordion, footer CTA) modelled on optlify.vercel.app's layout only — all copy written for this product. Added `.force-light`-scoped marketing display type sizes (text-6xl/7xl/8xl). Redirects repointed to /dashboard. Verified: tsc + build pass, no horizontal scroll at 360/768/1024/1440, `/dashboard` still bounces to /login signed out, `/` renders for both signed-in and signed-out. | app/src/app/(marketing)/{layout,page}.tsx, app/src/app/(app)/dashboard/page.tsx (moved from (app)/page.tsx), app/src/components/marketing/{primitives,marketing-nav,app-mock,pricing,faq,marketing-footer}.tsx, app/src/app/globals.css, app/src/lib/auth.config.ts, app/src/proxy.ts, app/src/actions/auth.ts, app/src/components/auth/{safe-callback-url.ts,register-form.tsx}, app/src/components/shell/nav-items.ts, memory/map.md, memory/decisions.md

2026-09-12 | codex | Final visual pass after screenshot review: ticket creation now reads as a stacked workflow with a wide work-description field, inputs use quiet surface fills with focus-only emphasis, and the timeline empty state uses the same layered palette without an outer outline. Dashboard remains intentionally empty. | app/src/components/work-log/ticket-search.tsx, app/src/components/work-log/work-log-timeline.tsx, app/src/components/ui/input.tsx, memory/design.md

2026-09-12 | codex | Reworked the Work Log editor visually: softer indigo/slate light+dark surfaces, larger rounded field boxes, redesigned daily-capture header, two-column editor, sticky ticket progress/status glance panel, removed Add Meeting controls, and added an explicit “What did you do?” field while attaching a ticket. | app/src/app/globals.css, app/src/app/layout.tsx, app/src/components/ui/input.tsx, app/src/components/ui/textarea.tsx, app/src/components/ui/select.tsx, app/src/components/work-log/**, memory/design.md

2026-09-12 | codex | Refined the Work Log from screenshot feedback: reduced visible borders, removed nested outlined boxes, changed the canvas to a mist/slate palette, flattened meeting and ticket surfaces, and changed ticket creation to a responsive stacked/grid layout. | app/src/app/globals.css, app/src/components/work-log/**, app/src/components/ui/input.tsx, memory/design.md

2026-09-12 | codex | Added the Work Logs listing/create flow, autosaving editor, and read-only timeline route. Restored the documented indigo-on-slate token palette after the live CSS had drifted to teal, and fixed the editor date hydration mismatch. Verified create-to-editor navigation, timeline rendering, targeted ESLint, TypeScript, and diff checks. | app/src/app/(app)/work-logs/**, app/src/components/work-log/create-work-log-form.tsx, app/src/components/work-log/work-log-editor.tsx, app/src/components/work-log/work-log-timeline.tsx, app/src/app/globals.css, memory/map.md

2026-09-12 | codex | Kept the dashboard intentionally empty (no dashboard data reads/cards) and aligned the light status accent tokens with the documented palette. Verified the empty dashboard in the browser; lint remains blocked by pre-existing work-log hook errors and the production build hit a Turbopack process-permission panic. | app/src/app/(app)/page.tsx, app/src/app/globals.css, memory/map.md

2026-09-12 | frontend-dev | App shell + primitives + auth screens: Inter/JetBrains Mono fonts, flash-free class-based dark mode (pre-hydration inline script + localStorage) with a 3-way theme toggle; primitives (Button/Input/Textarea/Select/Card/Badge/TicketId/TicketStatusBadge/PriorityIndicator/Field/EmptyState/ConfirmationDialog/sonner Toaster); AppSidebar (docked+56px rail at lg, overlay drawer below, cookie-persisted), TopNavigation, Menu, ⌘K SearchCommand, AppShell; /login + /register wired to the real server actions with inline per-field errors; stub pages for every nav route. Verified at 360/768/1024 in both themes; tsc + build + lint clean. | app/src/app/layout.tsx, app/src/app/(app)/**, app/src/app/(auth)/**, app/src/components/**, .claude/launch.json, memory/map.md, memory/worklog.md, memory/decisions.md

2026-09-12 | claude | Agent Office redrawn as a real 2D office: inline-SVG floor with walls/windows/whiteboard/plants and a workstation + seated character per role (typing animation while working, ✓ when done, empty chair when idle); click a desk for detail. | office/index.html, memory/map.md

2026-09-12 | backend-dev | Data + auth layer: Prisma schema + init migration, NextAuth v5 credentials + proxy route protection, requireUser gate, ticket append/autosave logic, work log + meeting + CRUD server actions, demo seed | app/prisma/*, app/prisma.config.ts, app/src/lib/*, app/src/actions/*, app/src/proxy.ts, app/src/app/api/auth/[...nextauth]/route.ts
2026-09-12 | claude | Built Agent Office: live office-floor monitor of the agent team at localhost:4600, reading Claude Code subagent transcripts (role, current action, turns, files touched) + memory/worklog.md. Zero deps, read-only, design tokens mirrored from memory/design.md. | office/server.mjs, office/index.html, memory/map.md, memory/decisions.md

2026-09-12 | ui-designer | Defined full design system (Flat Design direction, indigo-on-slate palette, Inter + JetBrains Mono) and implemented Tailwind v4 tokens: semantic colors light+dark, 7 ticket status + 4 priority ramps, type/spacing/radii/shadow scales, focus ring, 360/768/1024/1440 breakpoints, class-based dark mode. All contrast pairs verified (body >= 4.5:1, borders/ring >= 3:1). | memory/design.md, memory/decisions.md, memory/map.md, app/src/app/globals.css
2026-09-12 | ui-designer | Specified the corrective homepage/auth direction: document-only three-stop scrolling, repeat-on-entry reveals in both directions with compositor-only enter/exit tokens, static low-cost background geometry, and distinct useful login versus registration supporting stories. Added the new motion tokens; component markup and logic remain for Frontend Dev. | app/src/app/globals.css, memory/design.md, memory/worklog.md
2026-09-12 | frontend-dev | Replaced the homepage nested scrollport with native three-section document snapping; fixed final-section navigation back to Overview; replaced continuous view timelines, parallax, blur, and looping decoration with replayable IntersectionObserver reveals and static token backgrounds; restored distinct responsive supporting content for login and registration. | app/src/app/(marketing)/layout.tsx, app/src/app/(marketing)/page.tsx, app/src/app/globals.css, app/src/components/marketing/marketing-nav.tsx, app/src/components/marketing/reveal-controller.tsx, app/src/components/marketing/primitives.tsx, app/src/components/auth/auth-shell.tsx, app/src/app/(auth)/auth-motion.css, memory/map.md, memory/decisions.md, memory/worklog.md
2026-09-12 | codex | Tightened login/register into a full-viewport composition: equal desktop halves, compact mobile support band, no page-level scrolling, and reduced narrow-screen form spacing while retaining useful distinct support content. | app/src/components/auth/auth-shell.tsx, app/src/components/auth/auth-motion.css, app/src/components/auth/{login-form,register-form}.tsx
2026-09-12 | codex | Reworked both auth pages into true full-height split screens: full primary-blue supporting half, centered form half, no floating/cropped registration card, and shared viewport-safe structure. | app/src/components/auth/auth-shell.tsx, app/src/app/(auth)/auth-motion.css
2026-09-12 | codex | Replaced login/register supporting copy with concise product-specific messaging about daily logs, ticket history, notes, and workspace setup; removed the previous continuity/setup phrasing. | app/src/components/auth/auth-shell.tsx
2026-09-13 | frontend-dev | Made `/dashboard` an authenticated empty route with no dashboard data reads; refreshed the authenticated shell contrast and work-log listing/create/timeline hierarchy with token-only surface styling for light and dark themes. | app/src/app/(app)/dashboard/page.tsx, app/src/components/{shell/{app-shell,app-sidebar,top-navigation},work-log/{create-work-log-form,work-log-timeline}}, app/src/app/(app)/work-logs/page.tsx, memory/{map,worklog}.md
2026-09-13 | codex | Replaced the flat Work Logs history list with a color-coded rolling 14-day sprint timeline anchored on 2 September; groups logs into 2–15, 16–29, etc. and renders the next calculated sprint as an empty upcoming state. | app/src/app/(app)/work-logs/page.tsx, memory/map.md, memory/worklog.md
2026-09-13 | codex | Flattened sprint entries into a connected timeline, removed the Today’s progress panel from the editor glance rail, and added explicit token-based borders to ticket cards and ticket creation results. | app/src/app/(app)/work-logs/page.tsx, app/src/components/work-log/{work-log-glance,ticket-search,ticket-work-card}.tsx, memory/map.md, memory/worklog.md
2026-09-13 | codex | Replaced the seven ticket states with the five sprint workflow stages (In Progress, Sent to QA, Ready for production, Released, Done), added a drag-and-drop board with persisted status updates plus keyboard-friendly selectors, and added a data migration mapping legacy statuses. | app/prisma/schema.prisma, app/prisma/migrations/20260913010000_ticket_sprint_statuses/migration.sql, app/src/components/tickets/ticket-board.tsx, app/src/components/ui/ticket-status-badge.tsx, app/src/actions/tickets.ts, memory/map.md, memory/worklog.md
2026-09-13 | codex | Changed Work Logs to show only the current sprint by default with a horizontal timeline, added a View older sprints link, and added explicit borders to create-log fields, ticket search, ticket section, and empty ticket work state. | app/src/app/(app)/work-logs/page.tsx, app/src/components/work-log/{create-work-log-form,work-log-editor,ticket-search}.tsx, memory/map.md, memory/worklog.md
2026-09-13 | codex | Replaced the Tickets placeholder with a responsive seven-column status board; each status uses its semantic theme color, counts, empty state, latest update preview, and work-log metadata. | app/src/app/(app)/tickets/page.tsx, memory/map.md, memory/worklog.md
2026-09-13 | codex | Redesigned the work-log editor with a structured bordered capture header, semantic meeting accents, clearer ticket surfaces, and token-based progress/status cards; removed forced-light wrappers so auth and marketing routes inherit the shared dark theme, and moved ticket history loading to the interaction handler. | app/src/components/work-log/{work-log-editor,meeting-section,ticket-work-card,work-log-glance}.tsx, app/src/app/{layout.tsx,(auth)/layout.tsx,(marketing)/layout.tsx}, memory/map.md, memory/worklog.md
2026-09-13 | codex | Replaced the blue-indigo dark surface ladder with a neutral charcoal ladder while retaining blue as the action/status accent; updated the browser dark theme color to match. | app/src/app/globals.css, app/src/app/layout.tsx, memory/design.md, memory/worklog.md
2026-09-13 | codex | Fixed sprint timeline width leakage that horizontally shifted/cropped the authenticated shell by constraining app/main overflow and keeping horizontal scrolling inside the timeline. | app/src/components/shell/app-shell.tsx, app/src/app/(app)/work-logs/page.tsx, app/src/app/globals.css, memory/worklog.md
2026-09-13 | codex | Fixed the authenticated sidebar losing its sticky top section by switching the shell root from horizontal scrolling to overflow clipping; wide timeline content remains contained while the sidebar stays pinned. | app/src/components/shell/app-shell.tsx, memory/worklog.md
2026-09-13 | codex | Tightened sprint timeline entries into compact bordered cards with bounded widths, separated marker/card spacing, truncated date labels, and nowrap metric chips so timeline content no longer overlaps. | app/src/app/(app)/work-logs/page.tsx, memory/worklog.md
2026-09-13 | codex | Reworked the ticket board into collapsible status accordions with compact ticket rows; ticket details and status controls expand on demand while drag-and-drop still targets each status section. | app/src/components/tickets/ticket-board.tsx, memory/worklog.md
2026-09-13 | codex | Simplified sprint timeline entries to show only the date and title, removed metadata that was contributing to clipping, and reversed the log order so newest entries appear first. | app/src/app/(app)/work-logs/page.tsx, memory/worklog.md
2026-09-13 | codex | Prevented new weekend work logs, defaults the create form to the latest weekday, disables weekend creation, and opens an existing log for a selected date instead of creating a duplicate; the server action enforces the same rules. | app/src/components/work-log/create-work-log-form.tsx, app/src/actions/worklog.ts, app/src/app/(app)/work-logs/page.tsx, memory/worklog.md
2026-09-13 | codex | Clarified current versus previous sprint styling: current sprint uses the primary blue treatment, while older sprints rotate deterministically through theme-matched purple, green, amber, and neutral tones that work in light and charcoal dark mode. | app/src/app/(app)/work-logs/page.tsx, memory/worklog.md
2026-09-13 | codex | Simplified collapsed ticket accordion rows to show only the ticket number and title; status, updates, work-log count, and status controls remain available after expansion. | app/src/components/tickets/ticket-board.tsx, memory/worklog.md
2026-09-13 | codex | Reworked the create-work-log form into a single aligned desktop row for date, title, and action; moved helper text below the row and strengthened token-based borders for light and dark themes. | app/src/components/work-log/create-work-log-form.tsx, memory/worklog.md
2026-09-13 | codex | Reduced sprint timeline card widths and added scrollbar breathing room so the horizontal viewport no longer ends tightly against or visually crops a card. | app/src/app/(app)/work-logs/page.tsx, memory/worklog.md
2026-09-13 | codex | Modernized the read-only work-log timeline with a structured header, date/update chips, connected event rail, bordered event panels, and distinct meeting/ticket accents using theme tokens. | app/src/components/work-log/work-log-timeline.tsx, memory/worklog.md
2026-09-13 | codex | Split the read-only timeline into separate Meetings and Tickets accordions and added an At a glance rail with meeting/ticket counts plus ticket status details; the editor glance now uses the real meeting count too. | app/src/components/work-log/{work-log-timeline,work-log-glance,work-log-editor}.tsx, app/src/app/(app)/work-logs/[workLogId]/page.tsx, memory/worklog.md
2026-09-13 | codex | Added Motion Primitives-inspired token-native motion across authenticated surfaces: page reveals, staggered sprint/meeting/ticket/event lists, animated disclosure content, and compositor-only hover lift with reduced-motion fallbacks; also linked glance ticket anchors to timeline cards. | app/src/app/globals.css, app/src/components/{shell/page-header,tickets/ticket-board,work-log/{meeting-section,work-log-timeline}}, app/src/app/(app)/work-logs/page.tsx
2026-09-13 | codex | Fixed blank login/register screens caused by auth entrance elements starting at opacity zero while their easing variables were scoped only to the removed force-light wrapper; auth motion variables now exist at root scope so both split-screen forms and left support panels render and animate. | app/src/app/(auth)/auth-motion.css

2026-09-14 | claude | Re-aligned every field and button with design.md §7/§9 after the Resources form drifted: inputs/selects/textareas now ship a real `border-border` edge on `bg-surface` at `rounded-md` (6px) and 36px, so call sites stopped patching `border-border-strong bg-surface`; button size scale un-inverted (sm 32 / md 36 / lg 40, default md) with `rounded-md` + 1px press translate; auth inputs re-radiused to match; meeting notes textarea given a real edge instead of `border-transparent`. Also fixed the global `:focus-visible` rule, which set `border-radius: var(--radius-sm)` and so reshaped the focused element itself. Verified Resources, Work Logs, and the editor in light and dark; tsc + eslint clean. | app/src/app/globals.css, app/src/components/ui/{button,input,select,textarea}.tsx, app/src/components/auth/auth-field.tsx, app/src/components/resources/resource-library.tsx, app/src/components/work-log/{meeting-section,create-work-log-form,ticket-search}.tsx, memory/worklog.md, memory/map.md

2026-09-14 | claude | Rebranded off blue to teal-on-graphite (skill's Productivity Tool palette, user-chosen) and made the sidebar permanently dark via its own `--c-sidebar*` token family; `ThemeToggle` gained `tone="sidebar"`. Rebuilt the button system: six variants (primary/subtle/secondary/outline/ghost/danger), four sizes (28/32/36/40), `block`, press translate, and converted the remaining hand-rolled link/copy buttons onto it. Normalised app card radii onto `rounded-lg` (icon tiles `rounded-md`). Added CSS scroll-timeline reveals (`.scroll-reveal`, `.scroll-reveal-item`) behind an `@supports` + reduced-motion guard, using `translate`/`scale` so hover lift still composes. Added a history-aware back button at the far left of the top bar. Verified Work Logs, Resources, timeline and editor in light and dark; tsc + eslint clean, no console errors. | app/src/app/globals.css, app/src/components/shell/{app-sidebar,top-navigation,back-button}.tsx, app/src/components/theme/theme-toggle.tsx, app/src/components/ui/button.tsx, app/src/app/(app)/work-logs/page.tsx, app/src/components/{work-log,resources,tickets}/**, memory/{decisions,design,map,worklog}.md

2026-09-14 | claude | Added Follow-ups: FollowUp + FollowUpUpdate models (append-only updates, migration 20260914082054_follow_ups), lib/actions with per-user scoping and Zod validation on every mutation, and a /follow-ups board grouping open threads into Overdue/Today/Upcoming/undated with Done collapsed. Each card expands to the full dated record of what was said and where, plus append-an-update, mark done/reopen, reschedule and delete. Also swapped the UI face to Plus Jakarta Sans + JetBrains Mono via next/font — `--font-sans` had named "Manrope" with nothing loading it, so the whole app had been rendering in the system fallback. Verified create → append → mark done end to end in the browser; tsc + eslint clean. | app/prisma/schema.prisma, app/prisma/migrations/20260914082054_follow_ups/, app/src/lib/{follow-ups,validation}.ts, app/src/actions/follow-ups.ts, app/src/app/(app)/follow-ups/page.tsx, app/src/components/follow-ups/follow-up-board.tsx, app/src/components/shell/nav-items.ts, app/src/app/layout.tsx, app/src/app/globals.css, memory/{project,map,design,worklog}.md

2026-09-14 | sanity-checker + claude | Sanity pass on the teal/graphite redesign found 3 high + 7 medium; all fixed. HIGH: form controls and `Button variant="secondary"` used `border-border` (1.23:1 light / 1.51:1 dark) where SC 1.4.11 needs 3:1 — moved to `border-border-strong`; `overflow-hidden` on the resources list wrapper clipped the focus ring on all 11 rows; the top bar's "New work log" label used `sm:`, which generates NO CSS (globals.css resets breakpoints to xs/md/lg/xl), so the label had never rendered. MED: skip link, `Button variant="subtle"` hover and `Badge tone="primary"` moved off `--c-primary` onto `primary-strong`/`accent-text` for AA; theme-toggle frame on the rail moved to `sidebar-subtle` (was 1.26:1); `#app-sidebar` now scopes `--c-ring` to the bright teal (light ring was 2.69:1 on an active row); `<a><Button></a>` on /work-logs replaced with `Link + buttonVariants`; theme toggle is 44x44 below md and the resources Copy button no longer abuts its row. Clean: no horizontal scroll at any width, no element stranded by the scroll reveals, no leftover blue, no layout break from the font swap. | app/src/components/ui/{input,button,badge}.tsx, app/src/components/auth/auth-field.tsx, app/src/components/shell/{app-shell,top-navigation}.tsx, app/src/components/theme/theme-toggle.tsx, app/src/components/resources/resource-library.tsx, app/src/app/(app)/work-logs/page.tsx, app/src/app/globals.css, memory/{map,worklog}.md

2026-09-14 | claude | Generalised Follow-ups into Tracker (/follow-ups -> /tracker, nav "Tracker"). Added `EntryKind` (FollowUp | Task | Note) to the entry model and made `person` nullable, since only a follow-up is addressed to someone; a type dropdown now drives which fields the form shows, what they are called, and how the entry is grouped — Notes get their own section and are never dated (enforced in the Zod refinement, not just hidden in the UI), Tasks land in Overdue/Today/Upcoming by due date. Added an All/Follow-ups/Tasks/Notes filter. Verified all three kinds end to end in the browser. Migration 20260914084541_tracker_entry_kinds. NOTE: `prisma generate` does NOT reach the running dev server — restart it after every migration or the page 500s with "Unknown field". | app/prisma/schema.prisma, app/prisma/migrations/20260914084541_tracker_entry_kinds/, app/src/lib/{follow-ups,validation}.ts, app/src/actions/follow-ups.ts, app/src/app/(app)/tracker/page.tsx, app/src/components/tracker/tracker-board.tsx, app/src/components/shell/nav-items.ts, memory/{project,worklog}.md

2026-09-14 | claude | Removed the Ticket field from the Tracker composer at the user's request. The `ticketKey` column and its card chip stay, so entries created while the field existed still render theirs — it is simply no longer settable. Verified the Follow-up, Task and Note forms and a round-trip save. | app/src/components/tracker/tracker-board.tsx, memory/worklog.md

2026-09-14 | claude | Ported Nexa's Notes into Personal Workspace whole: Note -> NoteSection -> NotePage with description, react-icons icon pair, favourite and soft-delete trash; TipTap rich text per page. Dropped the old flat Note model and its 9 seeded rows (user approved), rewrote both seeds to build notebooks, and added /notes, /notes/new, /notes/[noteId] and /notes/[noteId]/edit in this app's own shell rather than Nexa's markup. Editor components ported unchanged behind a `note-field.tsx` adapter that keeps Nexa's Field contract on this app's tokens. New deps: @tiptap/*, lowlight, react-icons, framer-motion. Verified end to end — created a note with a section, a page and rich-text body, and it saved and rendered with the table of contents. tsc + eslint clean. | app/prisma/schema.prisma, app/prisma/migrations/20260914085535_notes_as_notebooks/, app/prisma/{seed,seed-bulk}.ts, app/src/lib/{notes,note-store,note-icons,validation}.ts, app/src/actions/{notes,content}.ts, app/src/components/notes/**, app/src/app/(app)/notes/**, app/src/components/shell/nav-items.ts, memory/{decisions,worklog}.md

2026-09-14 | claude | Added dummy notes, authored as HTML. `prisma/seed-notes.ts` (`npm run db:seed:notes`) writes 3 notebooks / 8 pages — a deploy runbook, a Postgres reference and an interview loop — exercising headings, bold/italic, inline code, highlight, ordered/unordered lists, checklists, blockquotes, syntax-highlighted code blocks and a table. Pages are written as plain HTML and converted with `generateJSON` from `@tiptap/html/server` using the SAME extension list the editor runs, so anything the extensions don't support is dropped at seed time and what lands is exactly what the editor round-trips. Re-runnable: deletes its own notes by title first. Two fixes this surfaced: the ported components still used Nexa's token vocabulary (bg-sheet, border-line-light, text-muted-light, black/white alpha borders) which generates NO CSS here, so panels had no background — swept onto this app's tokens; and the `.note-prose` stylesheet (~400 lines: headings, lists, checklists, tables, code blocks, lowlight theme) had never been ported, which is why rich text rendered as plain paragraphs. Ported it with Nexa's `--color-*` vars remapped onto our `--c-*`. Verified code blocks, tables and checklists in both themes. | app/prisma/seed-notes.ts, app/package.json, app/src/app/globals.css, app/src/components/notes/*.tsx, memory/worklog.md

2026-09-14 | claude | Removed the sidebar. Navigation is now a dark graphite pill across the top (`shell/app-nav.tsx`), built to the homepage nav's design — rounded-full, hairline top/left edge, 4px brand bottom/right edge — with sections centred in a 3-column grid from md and collapsing into a sheet below it on phones. Deleted app-sidebar.tsx, top-navigation.tsx, shell-context.tsx and constants.ts; the authenticated layout no longer reads a collapse cookie. Rescoped the focus-ring override from #app-sidebar to #app-nav, retuned scroll-padding-top 80 -> 96px, and re-toned the back button and mobile sheet for the dark ground. Fixed two defects found while verifying: the date and account block wrapped to a second row (four children in a three-column grid — brand and back are now one item), and the nav state closed itself in an effect (now derived from pathname, no effect). Verified at 375 and 1440 in both themes, no horizontal scroll. tsc + eslint clean. | app/src/components/shell/{app-nav,app-shell,back-button}.tsx, app/src/app/(app)/layout.tsx, app/src/app/globals.css, memory/{decisions,worklog}.md

2026-09-14 | claude | Active nav item is now an underline rather than a filled chip — a 2px accent rule under the label on the pill, and an underlined label in the mobile sheet. The rule is always in the DOM and only fades, so nothing reflows on navigation. | app/src/components/shell/app-nav.tsx, memory/worklog.md

2026-09-14 | claude | Rebalanced colour so teal marks only actions and current state. Neutralised every decorative icon tile (bg-surface-3/text-text-muted) and every section eyebrow label (text-text-subtle) across work logs, tracker, notes, resources, tickets and the coming-soon page. Removed the sprint panels' tinted shells entirely — including the rotating violet/green/amber/stone palette for older sprints — leaving a plain surface whose only accent is today's timeline marker; `today` is pinned once per render so the marker cannot land on two days across midnight. Verified current + history views and the notes list. tsc + eslint clean. | app/src/app/(app)/work-logs/page.tsx, app/src/app/(app)/notes/**, app/src/components/{work-log,resources,tracker,tickets,shell}/**, memory/{decisions,worklog}.md

2026-09-14 | claude | Built the real dashboard at /dashboard (first nav item): today's log with a continue/start action, overdue + due-today tracker entries, sprint counts, tickets by workflow stage as proportional rows, recent ticket work, and library counts — all real reads via the new lib/dashboard.ts. Extracted the sprint calendar into lib/sprint.ts because the dashboard and the Work Logs page had each derived it and disagreed by a day, and fixed a UTC/local shift where `toDateOnly` read UTC components off a local-midnight Date. Aligned the sprint log count with the Work Logs page's weekday-only filter (a plain count said 10 where the page showed 8). Also lifted the nav graphite off near-black (#0f1115 -> #1b1f27) with muted/subtle re-tuned to hold 7.7:1 and 4.6:1 on the new ground. Verified at 375 and 1440, no horizontal scroll; tsc + eslint clean. | app/src/lib/{dashboard,sprint}.ts, app/src/app/(app)/dashboard/page.tsx, app/src/app/(app)/work-logs/page.tsx, app/src/components/shell/nav-items.ts, app/src/app/globals.css, memory/{decisions,worklog}.md
2026-09-20 | codex | Reworked public login/register into the homepage's warm-paper/graphite palette with paired crisp light/dark workspace photography, matching sam. brand/theme control, and accessible high-contrast form controls. Removed the old teal mock progress/map visuals. Fixed desktop registration height so the full form remains within the viewport with contained overflow only when needed. Added scroll-aware animated active navigation for the public homepage, removed the handwritten hero note, and refined CTA/button states. Verified lint, production build, desktop/mobile auth renders, both themes, and menu section tracking. | app/src/components/auth/auth-shell.tsx, app/src/app/(auth)/auth-motion.css, app/src/components/portfolio/portfolio-nav.tsx, app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css
2026-09-20 | codex | Removed navigation chrome from auth pages (wordmark and theme control remain only), added a visibly animated scroll-state treatment to the public navbar, swapped the footer above the contact prompt, and made the homepage header action session-aware: signed-out visitors go to /login while signed-in users go to /dashboard. Verified active section and nav transform after scrolling, plus lint and production build. | app/src/components/portfolio/portfolio-nav.tsx, app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css
2026-09-20 | codex | Moved the contact/footer section before the coast quote so the “Have an idea…” content leads into the landscape. Improved dark coast readability with a restrained brightness/contrast/saturation lift and reduced right-side shade opacity; verified rendered section order, dark-mode filter, lint, and build. | app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css
2026-09-20 | codex | Changed auth wordmark/theme controls from a separate header band to an absolute overlay on the full-viewport split layout, preserving readable focus states and adding mobile top padding so forms do not collide with the overlay. Verified dark sign-in render plus lint and production build. | app/src/app/(auth)/auth-motion.css
2026-09-20 | codex | Added a shared PortfolioScrollController so every same-page homepage link (header, mobile nav, hero, footer, and Skiper secondary link) scrolls to its section with smooth Motion-friendly behavior and reduced-motion fallback. Verified About navigation updates the hash and lands at the section; lint and build pass. | app/src/components/portfolio/portfolio-scroll-controller.tsx, app/src/components/portfolio/portfolio-home.tsx
2026-09-20 | codex | Moved the footer to the final page position after the coast quote and gave it a dedicated dark pf-card surface with inverse text, responsive inner grid, and preserved footer navigation. Contact prompt now sits above the quote. Verified DOM order, dark footer background, responsive grid, lint, and build. | app/src/components/portfolio/portfolio-home.tsx, app/src/app/banner/banner.css
2026-09-20 | codex | Renamed the visible product brand from Sam/Personal Workspace to WorkNest across homepage, auth, marketing metadata/navigation/footer, app chrome, banner route, and dashboard mock labels. Removed remaining Sam/Personal Workspace strings from app source; lint and production build pass. | app/src/components/portfolio/portfolio-home.tsx, app/src/components/auth/auth-shell.tsx, app/src/app/layout.tsx, app/src/app/(marketing)/page.tsx, app/src/components/{shell,marketing,banner}/*
2026-09-20 | codex | Fixed the quote/footer regression after removing the quote side label: coast quote now uses a single-column layout so it stays right-aligned, and the footer wrapper no longer inherits the three-column footer grid, preventing the nested shell from collapsing and wrapping copyright text. Verified full-width 3-column footer sizing at 1280px and lint. | app/src/app/banner/banner.css
2026-09-20 | codex | Fixed public homepage menu navigation by scrolling directly to section IDs, calculating active state from document geometry, and preserving modifier-click behavior. Replaced public section reveals with replayable GSAP animations that respect reduced-motion preferences and keep text readable before hydration. Verified lint and production build. | app/src/components/portfolio/{portfolio-nav,portfolio-scroll-controller,portfolio-reveal}.tsx
2026-09-20 | codex | Fixed the homepage Notes menu item so it targets the public Notes & code ideas section inside Workspace instead of navigating to the protected `/notes` route and bouncing signed-out visitors to login. Added the Notes marker to desktop and mobile active navigation. | app/src/components/portfolio/{portfolio-nav,portfolio-home}.tsx
2026-09-20 | codex | Extended GSAP text motion across all public homepage content outside the header and footer: headings, paragraphs, card labels, buttons, links, quote text, and the scroll cue now stagger into place and replay on re-entry, with readable defaults and reduced-motion support. | app/src/components/portfolio/{portfolio-reveal,portfolio-home}.tsx
2026-09-20 | codex | Re-themed the authenticated WorkNest shell to match the homepage's warm paper/graphite visual system in light and dark modes, including typography rhythm, neutral surfaces, cream/graphite actions, and quieter navigation chrome. Verified the rendered Work Logs screen plus lint and production build. | app/src/components/shell/{app-shell,app-nav}.tsx, app/src/app/globals.css
2026-09-20 | codex | Extended the homepage redesign across authenticated page primitives: larger editorial page headers, warmer spacing, pill-shaped actions, softer form controls, and more generous empty states. Because these are shared primitives and shell tokens, Dashboard, Work Logs, Notes, Resources, Tracker, and deferred pages now share the same WorkNest visual language. Verified the rendered dashboard plus lint and production build. | app/src/components/shell/page-header.tsx, app/src/components/ui/{button,input,empty-state}.tsx, app/src/app/globals.css
2026-09-20 | codex | Completed the authenticated visual redesign: introduced a restrained warm accent so the app is not monochrome, switched primary actions back to homepage-style graphite, removed the separate dark nav pill, and matched the homepage's flat editorial header with underline-only active navigation and accessible focus treatment. Verified the rendered Resources page plus lint and production build. | app/src/components/shell/app-nav.tsx, app/src/components/ui/button.tsx, app/src/app/globals.css
2026-09-20 | codex | Fixed dashboard regressions: removed the thick warm Today-card edge and ornaments, used a light inverse action, refined the Focus empty state, made the shared authenticated header opaque and sticky over scrolled sections, restored nav focus, and added reduced-motion-aware GSAP reveals to dashboard sections and library cards. Verified rendered light/dark scrolled dashboard, lint, and production build. | app/src/app/(app)/dashboard/page.tsx, app/src/components/dashboard/dashboard-card-motion.tsx, app/src/components/shell/{app-nav,back-button}.tsx, app/src/app/globals.css, memory/{map,design,decisions,worklog}.md
2026-09-20 | codex | Reworked `/dashboard` onto a route-scoped white daylight canvas with borderless tonal panels, one sharp Work Log day/night photograph in the Today card, meaningful sprint-day SVG bars, a compact recent-update count, and ruled library links. Verified the rendered desktop light/dark and 390px phone layouts, restored the user's light theme, ran the design detector, and passed lint plus production build. | app/src/app/(app)/dashboard/page.tsx, app/src/app/globals.css, memory/{design,decisions,worklog}.md
2026-09-20 | codex | Replaced the narrow Work Log photo crop with a new screen-facing, cool-daylight workspace image in a full-width Today split feature. Removed dusty brown from the dashboard with route-scoped blue-slate tokens, kept only Focus tinted, and opened Sprint/Recent into ruled editorial sections. Preserved live data/routes; verified rendered desktop light/dark and 390px geometry, lint and production build. | app/public/Images/portfolio/dashboard-daylight.webp, app/src/app/(app)/dashboard/page.tsx, app/src/app/globals.css, memory/{design,decisions,worklog}.md
2026-09-20 | codex | Refined the dashboard shortcuts into bordered Notes, Resources, and Sprint logs actions with live counts and arrow icons, enlarged the greeting typography, and set dashboard/Work Logs dark mode to the requested #101012 graphite canvas with matching card shades. Browser-checked desktop and 390px mobile layouts; lint/build pass and detector reports only pre-existing advisory literals. | app/src/app/(app)/dashboard/page.tsx, app/src/app/globals.css, memory/{design,decisions,worklog}.md
2026-09-20 | codex | Blended the dashboard Today photo into the text surface with a responsive gradient instead of a split edge, removed dashboard section rule contrast on the white canvas, and aligned the public About section to white light mode / the lower graphite dark tone. Lint and production build pass. | app/src/app/globals.css, app/src/app/banner/banner.css, memory/{design,worklog}.md
2026-09-21 | codex | Finished the surface unification: the public Workspace band now uses pure white with no top rule in light mode and the same near-black card canvas as the surrounding dark sections. Rendered-check confirms the dark band is continuous; dashboard Today blend also verified in the authenticated browser. Lint and production build pass. | app/src/app/banner/banner.css, memory/worklog.md
2026-09-21 | codex | Replaced the dashboard Today calendar/laptop photo with the paired light/dark notebook images, removed its date badge and outer border, and centered its copy, metrics, and actions. Centered the public contact CTA and removed its top border plus the contact/coast seam. Reset the local database and changed `db:seed` to delete all users/cascade-owned content before creating the single requested dummy account; verified exactly 1 user with 3 work logs, 4 tickets, 4 tasks, 1 note, 3 links, and 3 resources. Authenticated dashboard and public contact were browser-checked; lint and production build pass. | app/src/app/(app)/dashboard/page.tsx, app/src/app/{globals.css,banner/banner.css}, app/prisma/seed.ts, memory/{map,design,decisions,worklog}.md
2026-09-21 | codex | Left-aligned the complete dashboard Today content block—icon, heading, metrics, and actions—after the centered treatment was rejected. Lint passes. | app/src/app/(app)/dashboard/page.tsx, memory/{design,worklog}.md
2026-09-21 | codex | Matched the read-only work-log timeline to the dashboard/Work Logs theme by applying the missing route scope, removing the old warm accent, card shadows, eyebrow labels, and nested frames, then rebuilding the content as open ruled sections with soft tonal event rows and one sticky summary rail. Verified rendered desktop light and dark modes; lint, TypeScript, and production build pass. | app/src/app/(app)/work-logs/[workLogId]/page.tsx, app/src/components/work-log/{work-log-timeline,work-log-glance}.tsx, memory/{map,design,decisions,worklog}.md
2026-09-21 | codex | Unified every authenticated route under the dashboard's cool daylight/blue-slate light theme and `#101012` graphite dark theme by promoting those tokens to `.app-shell`. Removed remaining Notes hardcodes: the TOC now uses semantic surfaces/foregrounds without the thick active side tab, the editor toolbar follows the current canvas, and the save action uses shared sidebar tokens. Render-checked Tracker (including open composer), Notes, Resources, and dark Notes/editor states; swept all authenticated routes for successful rendering. Lint, TypeScript, production build, and the targeted Impeccable detector pass (only pre-existing global CSS advisories remain). | app/src/app/globals.css, app/src/components/notes/{NoteForm,NoteTableOfContents}.tsx, memory/{map,design,decisions,worklog}.md
2026-09-22 | codex | Redesigned the work-log editor into open ruled sections aligned with the shared cool daylight/graphite theme and replaced meeting/ticket textareas with a compact TipTap Markdown field. `* ` now creates a visible bullet, Ctrl/Cmd+B and Ctrl/Cmd+I format selections, toolbar actions cover lists/quote/code/undo/redo, and Markdown renders in timeline/history while existing string storage and autosave remain unchanged. Verified live dark/light desktop, 390px mobile, real bullet and bold keyboard behavior, lint, TypeScript, production build, and targeted design detection. | app/src/components/work-log/{markdown-editor,meeting-section,ticket-search,ticket-work-card,work-log-editor,work-log-glance,work-log-timeline,ticket-history,section-heading}.tsx, app/src/app/globals.css, memory/{map,design,decisions,worklog}.md
2026-09-22 | codex | Replaced the rejected four-editor grid with a complete master-detail work-log layout: graphite command header with live progress, compact meeting navigation, one strongly signposted writing canvas, no summary sidebar, and a full-width ticket stage. Strengthened editor borders, focus, caret, selection, and field labelling so inputs no longer resemble read-only panels. Verified populated/empty data, light/dark desktop, 390px mobile, zero horizontal overflow, lint, TypeScript, production build, and clean layout detector output. | app/src/components/work-log/{work-log-editor,meeting-section,markdown-editor}.tsx, app/src/app/globals.css, memory/{map,design,decisions,worklog}.md
2026-09-22 | codex | Lightened the work-log editor after the graphite treatment was rejected: replaced the dark command header with a white bordered title surface and pale metric strip, and changed the selected meeting from graphite to a restrained teal-tinted state. Kept the strong white writing canvas and master-detail layout. Verified rendered desktop and 390px mobile states, lint, TypeScript, and diff whitespace. | app/src/components/work-log/{work-log-editor,meeting-section}.tsx, memory/{design,worklog}.md

## 2026-09-24 — Work-log detail redesign
Rebuilt `/work-logs/[workLogId]`: removed Meetings/Tickets count tiles and "In this log" list; rail is now Current ticket status + Ticket history (`work-log-glance.tsx`, takes `workLogId`). Timeline (`work-log-timeline.tsx`) uses meeting card grid + ticket cards, no disclosures. New scoped indigo tokens in globals.css. Not visually verified (preview browser not signed in).
- 2026-09-24 (later): Edit page — removed meetings/tickets/autosave stats strip under the header, tightened vertical spacing (header, main gap 14→8, section pt 10→8), and switched both work-log pages to the neutral zinc + indigo palette.
- 2026-09-24 (v3): Work-log edit page restructured into section cards (compact header, Meeting notes card with header bar + nav/editor split, Ticket work card with search strip + ticket list). `SectionHeading` now renders a `.wl-card-head` bar. Stronger zinc/indigo tokens. Timeline/glance use `.wl-card`.
- 2026-09-24 (v4): Work Logs list page redesigned (bordered create card, sprint cards with header bars, grid of log cards replacing the horizontal rail). Removed the global `#create-work-log` rule that stripped its border. Dropped the grey canvas — page bg is white on all work-log pages per user.
- 2026-09-24 (v5): User palette (#353535 #3c6e71 #ffffff #d9d9d9 #284b63) applied app-wide via `.app-shell` tokens; removed `.work-logs-page` indigo override. Public homepage/auth unaffected.
- 2026-09-24 (v6): Added `--c-card` / `bg-card` token; Card, EmptyState, ComingSoon, Tracker, Tickets (+loading), Notes, Dashboard hero/focus and all work-log cards use it. Removed grey header bars (ticket board, ticket detail, wl-card-head). Nested items white.
- 2026-09-24 (v7): Removed all grey from the app shell — surfaces, cards, borders, hovers, neutral chips, overlay now teal/navy tints; page bg white (body too, mobile themeColor #fff).
- 2026-09-24 (v8): White cards + teal-shade borders, tints only on emphasis cards. Timeline page, sprint list and sidebar converted from cards to ruled lists/tables/rail. Status badges solid; Badge/tracker chips bordered or solid.
- 2026-09-24 (v9): Previous sprints collapsed behind a disclosure on /work-logs. text-muted #414141 / text-subtle #555555 (user: sublabels too light). Seeded tariboi36@gmail.com additively: +4 tickets (25542, STC-118, VER-2471, ASU-1352) and `seed-fill` 45 days → 30 logs, 66 ticket updates, 4 tracker entries (Aug 11 – Sep 23).
- 2026-09-24 (v10): Edit-page header = navy `.wl-hero` panel (token re-point for light-on-navy); row 1 date | save pill + View timeline, row 2 title | project field.
- 2026-09-24 (v11): Global `.app-shell main` padding scoped to `main#main` and reduced (nested <main> in editor caused a ~4rem gap; editor now uses <div>). Meeting editor pane top padding aligned with nav. Ticket history dates/times/log titles darker (text-sm, text/text-muted); 'View previous updates' toggles in accent-text.
- 2026-09-24 (v12): Edit header fields now visibly inputs — labelled 'Title' / 'Project / site', solid white `.wl-field` boxes (light tokens reset inside navy `.wl-hero`), teal focus ring.
- 2026-09-24 (v13): Meeting notes card has no header row — heading + 'N of 4' pill live at the top of the meeting nav column; editor pane p-5 with the editor stretching (flex-1) so top/bottom spacing is equal. Card border darkened to #8fb1b4.
- 2026-09-24 (v14): MUI adopted for all app form fields (see decisions.md). Notes forms, icon search, link popover converted too. `npm run build` passes.
- 2026-09-24 (v15): Tracker redesigned — at-a-glance count strip (Overdue/Today/Upcoming/No date, anchor links), underline tabs with counts, each bucket = one bordered list with coloured header bar (danger/teal/navy/navy-tint) and divider rows (no per-entry cards), expanded history as a navy-dot rail, Done behind a 'Show done' pill.
- 2026-09-24 (v16): Tracker at-a-glance strip rebuilt as 4 equal full-width sections (coloured top bar, label, big count, blurb; 2×2 on mobile, 4-up from md). Fixed dead sm: classes in tracker.
- 2026-09-24 (v17): Sprint table on /work-logs replaced by a sprint calendar grid with a days-logged progress bar (user asked for a different component).
- 2026-09-24 (v18): Dashboard Today hero photo → dashboard-daylight.webp (brighter, palette-matched); desktop text-panel fade shortened (56% wide, solid to 72%) so the photo isn't washed out.
- 2026-09-24 (v19): Dashboard hero photo → user-supplied dashboard-desk.webp (trimmed from screenshot). Desktop: photo fills right side from 34%, left edge mask-faded into the card tint; text panel has no overlay. Verified with a static mock.
- 2026-09-24 (v20): Dashboard hero card (light mode) recoloured to the photo's sampled light tone #f4e7d4 with warm border/dividers #d8c2a3, so the masked photo edge blends seamlessly. Exception to the teal/navy card rule, scoped to .dashboard-today-card.
- 2026-09-24 (v21): Dashboard hero → full-viewport full-bleed banner holding greeting + shortcuts + today block (today block vertically centred); nav (`.app-header`) transparent over it and solidifies on scroll; photo re-graded to teal/navy (dashboard-desk-teal.webp) with bg #d2e3df. Nav height is 4rem mobile / 5.5rem md — banner offsets use both.
- 2026-09-24 (v22): Removed the teal grade from the Dashboard photo (user: no blue overlay). Back to original dashboard-desk.webp on cream #f4e7d4. Deleted the unused teal-graded webp and draft SVG illustration.
- 2026-09-24 (v23): Dashboard banner photo replaced with user's new cool-toned image (dashboard-desk-cool.webp, border trimmed); banner bg #e0e8e5 sampled from it; old warm dashboard-desk.webp deleted.
- 2026-09-24 (v24): Removed the mask fade from the Dashboard banner photo (user: remove the blend).
- 2026-09-24 (v25): Fixed menu-on-banner: <main> used overflow-x-hidden, which also clips vertically and cut the banner off under the nav → now overflow-x-clip (app-shell.tsx). Banner photo gets a narrow soft edge only (10% md+, 3rem phones).
- 2026-09-24 (v26): Banner photo blend: reframed (background-position 100% 55%) + eased 7-stop mask over first 33% — seamless, verified on mock.
- 2026-09-24 (v27): Banner photo framed at 45% so the mug is fully clear; today block at true vertical centre (md: grid rows 1fr/auto/1fr, .dashboard-hero-inner = 100dvh - nav).
- 2026-09-24 (v28): Work-log timeline rail: ticket history is its own scroll area (fills the sticky rail up to 100dvh-8rem on lg, max 28rem below); slim palette scrollbar via .wl-scroll.
